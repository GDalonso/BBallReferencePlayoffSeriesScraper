YEAR_PATTERN = "[0-9]{4}"

# get every caracter and "-" in a string that ends with .html
# doesn't return the .html
SERIES_NAME_PATTERN = "([\w-]+).html$"

# used to parse the game date
STRPTIME_GAME_PATTERN = "%b %d%Y"
